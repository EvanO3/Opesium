//This file will hold the model of the user
interface AuthUser{
    // the ? mark infront of name means the param is optional
    name?:string ,
    email: string,
    password:string


}


export default AuthUser;